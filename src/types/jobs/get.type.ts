export type currenciesType = {
  code: string
  name: string
  symbol: string
  country: string
}

export type ProjectType = {
  id?: string;
  name?: string;
  title?: string;
};

export type LocationType = {
  name: string;
  code: string;
};
