export type commonType = {
  id: string;
  name: string;
};

export type ReasonType = {
  name: string;
  reason_name: string;
  reason_description: string | null;
  label: string;
};

export type commonInterviewType = {
  name: string;
};
